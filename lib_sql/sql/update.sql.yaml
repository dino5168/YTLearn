UPDATE_DIC_SQL: |
  UPDATE dic_sql
    SET sqlkey = :sqlkey,
        sqlvalue = :sqlvalue
  WHERE id = :id;

UPDATE_NAV_ITEM_ROLES: |
  UPDATE nav_item_roles SET nav_item_id = :nav_item_id, role_id = :role_id WHERE id = :id;
